{
  // Configuration section for the MCP server. You can override these
  // settings via environment variables using the ASP.NET Core convention
  // (e.g. MCP__ApiKey to set MCP.ApiKey). When an ApiKey is provided
  // the HTTP transport will require callers to present it in an
  // X-Api-Key header.
  "MCP": {
    "ApiKey": "sum-matrix"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}